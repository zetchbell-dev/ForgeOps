# Loki never calls the Kubernetes API in single-binary/filesystem mode
# (no ring discovery via K8s needed with `kvstore: inmemory` — see
# loki-configmap.yaml) — same reasoning as grafana/serviceaccount.yaml.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: loki
automountServiceAccountToken: false
