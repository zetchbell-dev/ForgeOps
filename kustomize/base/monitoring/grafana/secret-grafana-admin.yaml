# Shape only — provisioned out-of-band (External Secrets Operator / Sealed
# Secrets / SOPS), same convention as alertmanager-config-secret.yaml's
# `slack_api_url`. Never commit a real password here.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
type: Opaque
stringData:
  admin-user: "REPLACE_OUT_OF_BAND"
  admin-password: "REPLACE_OUT_OF_BAND"
